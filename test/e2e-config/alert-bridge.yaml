server:
  port: 8080
  host: "0.0.0.0"
  read_timeout: 10s
  write_timeout: 10s
  request_timeout: 5s
  shutdown_timeout: 5s

logging:
  level: info
  format: json

alertmanager:
  webhook_path: /webhook/alertmanager

slack:
  enabled: true
  bot_token: "xoxb-mock-token-e2e"
  signing_secret: "mock-signing-secret"
  channel_id: "C123456"
  app_id: "A123456"
  api_url: "http://mock-slack:9091/api/"

pagerduty:
  enabled: true
  api_token: "mock-api-token"
  routing_key: "mock-routing-key"
  service_id: "PSERVICE1"
  webhook_secret: "mock-webhook-secret"
  from_email: "alert-bridge-e2e@example.com"
  default_severity: warning
  api_url: "http://mock-pagerduty:9092"

alerting:
  deduplication_window: 5m
  resend_interval: 1h
  silence_durations:
    - 30m
    - 1h
    - 4h
    - 24h

storage:
  type: memory
